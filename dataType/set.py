# 집합자료형
s1 = set([1,2,3])
print(s1)
s2 = set("HelloPython")
print(s2) # 중복을 허용하지 않고 순서가 없다는 특징이 있다, "순서가 없다" == "인덱싱을 지원하지 않는다"
# 따라서 자료중복을 제거하기 위한 필터로 사용된다.
l1 = list(s1)
print(l1) # 여기까지 깃헙에 잔디심기 테스트를 위한 밑밥이었다! git config --list 에서 유저와 이메일을 변경함.